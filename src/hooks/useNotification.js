// Re-export the useNotification hook from the Notification component
export { useNotification } from '../components/Notification';

// This file exists to provide a consistent import path for components
// that expect to import useNotification from the hooks directory